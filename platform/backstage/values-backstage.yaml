app:
  title: Platform Backstage
  baseUrl: http://192.168.0.12:30007

backend:
  baseUrl: http://192.168.0.12:30007
  listen:
    port: 7007

  database:
    client: sqlite3
    connection: ':memory:'

integrations:
  github:
    - host: github.com
      token: ${GITHUB_TOKEN}

auth:
  providers: {}
